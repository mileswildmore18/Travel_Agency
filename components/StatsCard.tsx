// destructure the props
const StatsCard = ({
    headerTitle,
    total,
    lastMonthCount,
    currentMonthCount
                   }: StatsCard) => {
    return (
        <div>StatsCard</div>
    )
}
export default StatsCard
